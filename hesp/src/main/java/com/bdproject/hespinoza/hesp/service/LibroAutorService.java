package com.bdproject.hespinoza.hesp.service;

import java.util.List;
import java.util.Optional;

import org.springframework.stereotype.Service;

import com.bdproject.hespinoza.hesp.model.entity.LibroAutor;
//import com.bdproject.hespinoza.hesp.model.repository.LibroAutorRepository;

/*
@Service
public class LibroAutorService {

    private final LibroAutorRepository libroAutoRepo;

    public LibroAutorService(LibroAutorRepository eRepo) {
        this.libroAutoRepo = eRepo;
    }

    public List<LibroAutor> findAll() {
        return libroAutoRepo.findAll();
    }

    public Optional<LibroAutor> findById(Integer id) {
        return libroAutoRepo.findById(id);
    }

    public LibroAutor guardar(LibroAutor lb) {
        return libroAutoRepo.save(lb);
    }

    public void delete(LibroAutor libro) {
        libroAutoRepo.delete(libro);
    }

}
     */
